import { ReactNode } from "react";

/** Segmento de detalle — las modales intercepting viven en /users/@modal. */
export default function UserIdLayout({ children }: { children: ReactNode }) {
  return children;
}
